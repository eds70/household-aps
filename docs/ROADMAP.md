# Roadmap

План развития системы **APS Production Scheduler**.

---

## 📋 Содержание

- [Обзор](#обзор)
- [Таблица итераций 0–13.15](#таблица-итераций-01315)
- [Детали по итерациям](#детали-по-итерациям)
- [Планируемые фичи (Итерация 14+)](#планируемые-фичи-итерация-14)
- [Отменённые фичи](#отменённые-фичи)
- [Легенда](#легенда)

---

## Обзор

Проект развивается **итеративно**. Каждая итерация — законченный блок функциональности с тестами и документацией.

**Текущая версия:** 4.1.1
**Завершено итераций:** 19 (0–13.15)
**Следующая:** Итерация 14 — Встроенная справка пользователя

---

## Таблица итераций 0–13.15

| # | Итерация | Длит. | Приоритет | Статус |
|---|----------|-------|-----------|--------|
| 0 | Подготовка | 4 дня | 🔥 | ✅ |
| 1 | Цепочки рабочих центров | 2 нед | 🔥🔥🔥 | ✅ |
| 2 | Материальные ограничения и Advisor | 2 нед | 🔥🔥🔥 | ✅ |
| 3 | Сменное планирование и РМ мастера | 2 нед | 🔥🔥🔥 | ✅ |
| 4 | Перепланирование | 2 нед | 🔥🔥🔥 | ✅ |
| 5 | Лаборатория и блокировки | 1.5 нед | 🔥🔥 | ✅ |
| 5h | Hotfix: версии + tz + Gantt | 2 дня | 🔥🔥🔥 | ✅ |
| 6 | Люди как ресурс | 2 нед | 🔥🔥 | ✅ |
| 7 | Охлаждение с деградацией | 1.5 нед | 🔥 | ✅ |
| 7h | Hotfix: модель деградации охлаждения | 2 дня | 🔥🔥🔥 | ✅ |
| 8 | ЧЗ и интеграции | 2 нед | 🔥 | ✅ |
| 9 | Рефакторинг, A3 (реальный пересчет), C2 (drag-and-drop) | 2 нед | 🟡 | ✅ |
| 10 | Календарная постобработка, разбиение длинных задач | 2 нед | 🟡 | ✅ |
| 11 | Режимы смен, app_settings, pan/zoom в Ганте | 2 нед | 🟡 | ✅ |
| 12 | Multi-objective и what-if сценарии | 2 нед | 🟡 | ✅ |
| 13.3 | Аудит (объединённый журнал событий) | 1 нед | 🟡 | ✅ |
| 13.14 | Настройки, привязанные к плану (plan_settings) | 1 нед | 🟡 | ✅ |
| 13.15 | Исправление: снапшоты при создании плана | 2 дня | 🔥🔥 | ✅ |
| 13.16 | Унификация диалогов + UI-полировка | 2 дня | 🟡 | ✅ |
| 14 | Встроенная справка пользователя | 1 нед | 🟡 | ⏳ |

---

## Детали по итерациям

### Итерация 0 — Подготовка ✅

**Длительность:** 4 дня
**Приоритет:** 🔥

**Что сделано:**
- Эталонный тест-кейс из ТЗ (Раздел 4).
- Централизованная конфигурация через `organization_settings`.
- Feature-флаги для поэтапного внедрения.
- Structured logging планировщика.
- CI на GitHub Actions.

---

### Итерация 1 — Цепочки рабочих центров ✅

**Длительность:** 2 недели
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- Модуль `routing.py` — построение цепочек операций.
- Двухресурсные операции (`linked_equipment_id`).
- Слив: `реактор → линия` (DIRECT) или `реактор → танк → линия` (VIA_TANK).
- Замыв реактора после слива.
- `NoOverlap` по каждому ресурсу отдельно.
- Оптимизация setup-ограничений (270 вместо 9714).
- Корректный расчёт длительности слива.
- Makespan ~553 ч (было 1856 ч).

---

### Итерация 2 — Материальные ограничения и Advisor ✅

**Длительность:** 2 недели
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- Модуль `materials.py` — расчёт потребности в сырье.
- Модуль `advisor.py` — типы подсказок:
  - 🔴 `MATERIAL_SHORTAGE` — дефицит сырья.
  - 🟡 `UNDERLOAD` — неполная загрузка реактора.
  - 🔵 `ROUTE_MISMATCH` — VIA_TANK без танка.
  - 🔵 `EQUIPMENT_GAP` — простои оборудования.
- Модуль `feasibility.py` — оценка исполнимости.
- API: `GET /api/v1/schedule/advice`, `POST /api/v1/schedule/feasibility`.
- UI: панель Advisor с фильтрацией.
- Обнаружение дефицита отдушки (150 кг) и соли (1500 кг).

---

### Итерация 3 — Сменное планирование и РМ мастера ✅

**Длительность:** 2 недели
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- Таблица `shift`.
- Поля в `scheduled_task`: `shift_id`, `actual_qty`, `material_load_at`, `status`.
- Модуль `shifts.py`.
- API `shift.py` — 5 эндпоинтов.
- Frontend `ShiftPage.tsx` — рабочее место мастера.
- Пункт меню «Мастер смены».
- Все 282 задачи привязаны к сменам.

---

### Итерация 4 — Перепланирование ✅

**Длительность:** 2 недели
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- Модуль `rescheduler.py`.
- Типы изменений: `DELAY`, `BREAKDOWN`, `QTY_CHANGE`, `MANUAL`.
- Закрепление задач (`is_pinned`) и заморозка до `frozen_before`.
- Журнал `reschedule_log`.
- API `reschedule.py`.
- UI: диалог перепланирования.
- Сценарий «Аварийная остановка Р4 (25–28.09)».

---

### Итерация 5 — Лаборатория и блокировки ✅

**Длительность:** 1.5 недели
**Приоритет:** 🔥🔥

**Что сделано:**
- Поля в `batch`: `is_lab_blocked`, `lab_status`, `lab_block_reason`, `lab_blocked_at`, `lab_blocked_by`.
- Таблица `lab_analysis_log`.
- Модуль `lab.py` — 7 эндпоинтов.
- Планировщик исключает заблокированные партии.
- `build_routing(truncate_after_lab=True)`.
- UI `ShiftPage.tsx`: индикатор блокировки.
- UI `GanttPage.tsx`: 🔒 красная рамка + фильтр.
- Роли `LAB`, `MASTER`, `ADMIN`.

---

### Итерация 5h — Hotfix: версии + tz + Gantt ✅

**Длительность:** 2 дня
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- **Hotfix #1:** деактивация старых версий плана (`saver.py`).
- **Hotfix #2:** фильтрация по `schedule_version_id` (`shift.py`, `gantt.py`).
- **Hotfix #3:** таймзона в `by-date` (`shift.py`).
- **Hotfix #4:** `is_lab_blocked` в Ганте (`gantt.py`).
- Миграция данных `fix_versions_hotfix.sql`.

---

### Итерация 6 — Люди как ресурс ✅

**Длительность:** 2 недели
**Приоритет:** 🔥🔥

**Что сделано:**
- 4 пула операторов в `resource_pool`.
- Колонка `scheduled_task.operator_pool`.
- Колонка `resource_pool.updated_at`.
- UNIQUE-констрейнт `resource_pool (organization_id, type)`.
- `AddCumulative` для каждого пула.
- Модуль `plugins.py` — `OperatorPoolConstraint`, `LabConstraint`, `CoolingZoneConstraint`.
- Модуль `routing.py` — назначение пула.
- API `/api/v1/personnel` — 4 эндпоинта.
- Алгоритм `peak_concurrent`.
- UI страница «Персонал».
- Feature-флаги `enable_operator_pools`, `enable_manual_station`.

---

### Итерация 7 — Охлаждение с деградацией ✅

**Длительность:** 1.5 недели
**Приоритет:** 🔥

**Что сделано:**
- Миграция `add_10.sql`.
- Миграция `add_10b.sql` — колонка `scheduled_task.cooling_mode`.
- Настройки в `organization_settings`.
- Модель деградации в `core.py`.
- `CoolingDegradationConstraint` в `plugins.py`.
- API `/api/v1/gantt/` возвращает `cooling_mode`.
- Excel-экспорт.
- Advisor `COOLING_DEGRADATION`.
- UI `GanttPage.tsx`, `ShiftPage.tsx`, `PersonnelPage.tsx`.
- Тест-кейс ТЗ: 0 ложных `slow`.

---

### Итерация 7h — Hotfix: модель деградации охлаждения ✅

**Длительность:** 2 дня
**Приоритет:** 🔥🔥🔥

**Что сделано:**
- Исправлена модель деградации в `core.py`: `b_slow` жёстко связан с фактическим пересечением интервалов.
- `CoolingDegradationConstraint` больше не делает `AddNoOverlap(fast_intervals)`.

---

### Итерация 8 — ЧЗ и интеграции ✅

**Длительность:** 2 недели
**Приоритет:** 🔥

**Что сделано:**
- Миграция `add_11.sql`.
- Модуль `scheduler/cz.py`.
- API `/api/v1/cz` — 7 эндпоинтов.
- Идемпотентность.
- Порог завершения.
- Advisor `CZ_INCOMPLETE`.
- UI `CzPage.tsx`.
- UI `ShiftPage.tsx` — индикатор ЧЗ.
- UI `GanttPage.tsx`.
- UI `MainLayout.tsx`: пункт меню «Честный Знак».

---

### Итерация 9 — Рефакторинг, A3, C2 ✅

**Длительность:** 2 недели
**Приоритет:** 🟡

**Что сделано:**
- **A3:** реальное перепланирование.
- **C2:** drag-and-drop на Ганте.
- **A1:** удалён мёртвый код.
- **B1:** единая функция `find_shift_id_for_time`.
- **B2:** расширен `PERSONNEL_POOL_TYPES`.
- `REACTOR_OPERATOR.capacity` исправлен на 3.

---

### Итерация 10 — Календарная постобработка ✅

**Длительность:** 2 недели
**Приоритет:** 🟡

**Что сделано:**
- Новый модуль `calendar_postprocess.py`.
- Календарные ограничения убраны из CP-SAT.
- Топологическая сортировка.
- Учёт `NoOverlap`.
- Результат: `overlaps_after = 0`, `dependency_violations = 0`.
- Разбиение длинных `LINE_FILL` на подзадачи.
- Миграция `add_12.sql` — колонка `operation_name`.
- Изменения в `models.py`.

---

### Итерация 11 — Режимы смен, app_settings, pan/zoom ✅

**Длительность:** 2 недели
**Приоритет:** 🟡

**Что сделано:**
- **Шаг 4:** `shift_regenerator.py`.
- **Шаг 5:** миграция `add_13.sql` — `app_settings`.
- **Шаг 6:** `allow_weekend_work`.
- **Шаг 7:** UI-полировка, pan/zoom в Ганте.
- Миграция `add_14.sql`.
- `SettingsPage.tsx`.
- `ShiftPage.tsx` — фикс UX.
- `PlainContext.tsx` — фикс цикла ре-рендера.
- `GanttPage.tsx` — pan + zoom + drag.
- `index.css` — курсоры.

**Итоги:** 268 тестов, OPTIMAL за ~78 сек, makespan 12526 мин.

---

### Итерация 12 — Multi-objective и what-if ✅

**Длительность:** 2 недели
**Приоритет:** 🟡

**Что сделано:**
- **A. Multi-objective:**
  - Модуль `optimization.py`.
  - 5 компонентов целевой функции.
  - Нормализация.
  - `SettingsPage.tsx` — категория «Оптимизация».
  - Миграция `add_15.sql`.
- **B. What-if:**
  - Таблица `whatif_scenario` (миграция `add_16.sql`).
  - Модуль `whatif.py`.
  - Архитектура 2 транзакций.
  - 7 эндпоинтов API.
  - Async запуск.
  - 7 типов изменений.
  - UI `/whatif`.
  - Rollback гарантирован.
- Архитектурные патчи.
- 45 новых тестов.

**Итоги:** 321 тест, OPTIMAL за 7–25 сек.

---

### Итерация 13.3 — Аудит ✅

**Длительность:** 1 неделя
**Приоритет:** 🟡

**Что сделано:**
- Объединённый журнал событий.
- Единая точка входа для истории изменений.
- 4 источника: `material_stock_log`, `reschedule_log`, `lab_analysis_log`, `cz_scan_log`.
- API `/api/v1/audit` — 3 эндпоинта.
- UI `AuditPage.tsx`.
- Фильтры синхронизируются с URL.

---

### Итерация 13.14 — plan_settings ✅

**Длительность:** 1 неделя
**Приоритет:** 🟡

**Что сделано:**
- **БД:** миграция `add_21.sql`.
  - Таблица `plan_settings`.
  - Триггер `copy_app_settings_to_plan`.
  - Индексы.
- **Backend:**
  - `DataLoader(version_id=...)`.
  - `ProductionScheduler(version_id=...)`.
  - `settings_reader.py` — 3 функции с `version_id`.
  - `rescheduler.py`, `whatif.py`.
  - API `/api/v1/plan-settings` — 3 эндпоинта.
  - 6 API-модулей обновлены.
- **Frontend:**
  - `planSettingsApi`.
  - `PlanSettingsWizard.tsx` (9 шагов).
  - `SchedulePage.tsx` — кнопка «Настройки плана».
  - `MainLayout.tsx` — активный план.
- **Тесты:** +171, всего 492.
- **Гарантии:** изоляция, воспроизводимость, обратная совместимость, fallback, CASCADE DELETE, идемпотентность.

---

### Итерация 13.15 — Снапшоты при создании плана ✅

**Длительность:** 2 дня
**Приоритет:** 🔥🔥

**Проблема:**
При создании «пустого» плана через `POST /api/v1/schedule/versions` (например, из мастера `PlanSettingsWizard` в режиме create) снапшот-таблицы справочников (`equipment_snapshot`, `product_snapshot`, `operation_snapshot`, `calendar_snapshot`) **не заполнялись**. UI переключался в readonly-режим и показывал **пустые гриды** на всех страницах справочников.

**Что сделано:**

- **Новый модуль `backend/app/scheduler/snapshot.py`:**
  - `snapshot_all_catalogs(session, org_id, version_id)` — заполняет все 4 снапшот-таблицы.
  - `snapshot_exists(session, version_id)` — проверяет наличие снапшотов.
  - `_has_column(session, table, column)` — graceful-проверка схемы.
  - Идемпотентен (`ON CONFLICT DO NOTHING`).

- **API `schedule.py`:**
  - `POST /versions` вызывает `snapshot_all_catalogs`.
  - `GET /versions` возвращает `has_snapshot`.
  - Ответ содержит `snapshot_stats`.

- **`saver.py`:**
  - `_do_save` делегирует снапшоты в `snapshot_all_catalogs`.
  - Удалён inline SQL (4 INSERT'а).

- **Frontend:**
  - `PlainContext.tsx`: `PlanVersion.has_snapshot`, `CurrentPlan.has_snapshot`, `setPlan(id, name, hasSnapshot?)`, `currentPlanHasSnapshot`.
  - `SchedulePage.tsx`: иконка ⚠ + жёлтая подсветка строк для планов без снапшотов.
  - `GanttPage.tsx`: предупреждение вместо диаграммы для пустого плана.

- **Тесты:** +43, всего 535.
  - `test_snapshot.py` (18).
  - `test_schedule_create_version.py` (10).
  - `test_saver_uses_snapshot.py` (10).
  - Обновлён `test_plan_settings_models.py`.

**Итоги:**
- ✅ 535 passed, 0 failed.
- ✅ Новые планы сразу имеют снапшоты → справочники видны.
- ✅ Старые планы подсвечены ⚠, при открытии — предупреждение.
- ✅ Единый источник правды для снапшотов — модуль `snapshot.py`.

---

## Планируемые фичи (Итерация 14+)

### Итерация 14 — Встроенная справка пользователя ⏳

**Длительность:** 1 неделя
**Приоритет:** 🟡
**Статус:** В плане

**Планируемые фичи:**
- Встроенная справка в UI (контекстные подсказки).
- Раздел «Помощь» в меню.
- Поиск по справке.
- Ссылки на docs/.
- Туториал для новых пользователей.
- FAQ.

---

### Итерация 15 — Расширенный аудит и отчёты (планируется)

**Длительность:** 2 недели
**Приоритет:** 🟡

**Планируемые фичи:**
- Расширенные отчёты по производству.
- Экспорт в PDF.
- Дашборд с метриками.
- Графики загрузки оборудования.
- Отчёт по эффективности (OEE).

---

### Итерация 16 — HR-подсистема (планируется)

**Длительность:** 2 недели
**Приоритет:** 🟡

**Планируемые фичи:**
- ФИО операторов.
- График отпусков.
- Больничные.
- Квалификации.
- Привязка операторов к пулам.

---

### Итерация 17 — Интеграция с 1С (планируется)

**Длительность:** 3 недели
**Приоритет:** 🟢

**Планируемые фичи:**
- Синхронизация заказов.
- Синхронизация материалов.
- Синхронизация готовой продукции.
- API-интеграция.

---

### Итерация 18 — Мобильное приложение (планируется)

**Длительность:** 4 недели
**Приоритет:** 🟢

**Планируемые фичи:**
- React Native.
- Рабочее место мастера.
- Уведомления.
- Offline-режим.

---

## Отменённые фичи

### 1. Жёсткие материальные constraints в CP-SAT

**Причина:** сложность реализации, несовместимость с существующей моделью. Оставлены как предупреждения Advisor.

**Альтернатива:** Advisor `MATERIAL_SHORTAGE`.

---

### 2. Автоматическое перепланирование после блокировки партии

**Причина:** требует явного согласия пользователя (может сломать существующий план).

**Альтернатива:** ручной запуск перепланирования через UI.

---

### 3. Собственный solver

**Причина:** OR-Tools CP-SAT покрывает все потребности, собственная разработка — избыточна.

**Ссылка:** [ADR 0001](adr/0001-use-ortools-cp-sat.md).

---

### 4. Soft-delete справочников

**Причина:** снапшоты (`schedule_version`) уже решают задачу версионирования.

**Ссылка:** [ADR 0002](adr/0002-snapshot-tables-for-versioning.md).

---

### 5. `plan_settings` как поле в `schedule_version`

**Причина:** JSONB-поле не даёт валидации на уровне БД и гибкости для UI.

**Ссылка:** [ADR 0003](adr/0003-plan-settings-per-plan.md).

---

### 6. What-if через savepoint

**Причина:** savepoint не работает с async SQLAlchemy в нужном объёме, 2 транзакции надёжнее.

**Ссылка:** [ADR 0004](adr/0004-whatif-two-transactions.md).

---

### 7. Заполнение снапшотов через Python/API вместо модуля

**Причина:** дублирование логики между `saver.py` и `schedule.py`. Вынесено в единый `snapshot.py`.

**Реализовано в:** Итерации 13.15.

---

## Легенда

### Приоритеты

| Обозначение | Значение |
|-------------|----------|
| 🔥🔥🔥 | Критический |
| 🔥🔥 | Высокий |
| 🔥 | Средний |
| 🟡 | Низкий |
| 🟢 | Отложенный |

### Статусы

| Обозначение | Значение |
|-------------|----------|
| ✅ | Завершено |
| ⏳ | В плане |
| 🚧 | В работе |
| ❌ | Отменено |

---

## Ссылки

- [README.md](../README.md) — основная документация.
- [CHANGELOG.md](../CHANGELOG.md) — история изменений.
- [docs/ARCHITECTURE.md](ARCHITECTURE.md) — архитектура.
- [docs/adr/README.md](adr/README.md) — индекс ADR.
- [docs/DEVELOPMENT.md](DEVELOPMENT.md) — руководство разработчика.